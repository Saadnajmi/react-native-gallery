diff --git a/node_modules/react-native-linear-gradient/RNLinearGradient.podspec b/node_modules/react-native-linear-gradient/RNLinearGradient.podspec
index ab86e6c..07357f2 100644
--- a/node_modules/react-native-linear-gradient/RNLinearGradient.podspec
+++ b/node_modules/react-native-linear-gradient/RNLinearGradient.podspec
@@ -12,11 +12,12 @@ Pod::Spec.new do |s|
   s.license         = "MIT"
   s.author          = { "Brent Vatne" => "brentvatne@gmail.com" }
   s.ios.deployment_target = '9.0'
+  s.osx.deployment_target = '10.15'
   s.tvos.deployment_target = '9.0'
   s.source          = { :git => "https://github.com/brentvatne/react-native-linear-gradient.git", :tag => "v#{s.version}" }
   s.source_files    = 'ios/*.{h,m,mm}'
   s.preserve_paths  = "**/*.js"
-  s.frameworks = 'UIKit', 'QuartzCore', 'Foundation'
+  # s.frameworks = 'UIKit', 'QuartzCore', 'Foundation'
 
   # Use install_modules_dependencies helper to install the dependencies if React Native version >=0.71.0.
   # See https://github.com/facebook/react-native/blob/febf6b7f33fdb4904669f99d795eba4c0f95d7bf/scripts/cocoapods/new_architecture.rb#L79.
diff --git a/node_modules/react-native-linear-gradient/ios/RNLinearGradient.h b/node_modules/react-native-linear-gradient/ios/RNLinearGradient.h
index 52a14be..090e196 100644
--- a/node_modules/react-native-linear-gradient/ios/RNLinearGradient.h
+++ b/node_modules/react-native-linear-gradient/ios/RNLinearGradient.h
@@ -1,3 +1,5 @@
+#import <Foundation/Foundation.h>
+#import <React/RCTUIKit.h>
 #ifdef RCT_NEW_ARCH_ENABLED
 #import <React/RCTViewComponentView.h>
 
@@ -8,7 +10,7 @@
 @interface RNLinearGradient : RCTView
 #endif
 
-@property (nullable, nonatomic, copy) NSArray<UIColor *> *colors;
+@property (nullable, nonatomic, copy) NSArray<RCTUIColor *> *colors;
 @property (nullable, nonatomic, copy) NSArray<NSNumber *> *locations;
 @property (nonatomic) CGPoint startPoint;
 @property (nonatomic) CGPoint endPoint;
diff --git a/node_modules/react-native-linear-gradient/ios/RNLinearGradient.mm b/node_modules/react-native-linear-gradient/ios/RNLinearGradient.mm
index 509fc9b..d8de08a 100644
--- a/node_modules/react-native-linear-gradient/ios/RNLinearGradient.mm
+++ b/node_modules/react-native-linear-gradient/ios/RNLinearGradient.mm
@@ -12,7 +12,7 @@
 #endif
 
 #import <React/RCTConvert.h>
-#import <UIKit/UIKit.h>
+#import <React/RCTUIKit.h>
 #import <QuartzCore/QuartzCore.h>
 
 #import "RNLinearGradientLayer.h"
@@ -27,6 +27,19 @@ using namespace facebook::react;
 
 @implementation RNLinearGradient
 
+#if TARGET_OS_OSX
+- (instancetype)init
+{
+    self = [super init];
+    if (self) {
+        self.wantsLayer = true;
+        self.layer = [CAGradientLayer new];
+
+    }
+    return self;
+}
+#endif // TARGET_OS_OSX
+
 + (Class)layerClass
 {
   return [RNLinearGradientLayer class];
@@ -47,9 +60,9 @@ using namespace facebook::react;
     NSMutableArray *colors = [NSMutableArray arrayWithCapacity:colorStrings.count];
     for (NSString *colorString in colorStrings)
     {
-        if ([colorString isKindOfClass:UIColor.class])
+        if ([colorString isKindOfClass:RCTUIColor.class])
         {
-            [colors addObject:(UIColor *)colorString];
+            [colors addObject:(RCTUIColor *)colorString];
         }
         else
         {
diff --git a/node_modules/react-native-linear-gradient/ios/RNLinearGradientLayer.h b/node_modules/react-native-linear-gradient/ios/RNLinearGradientLayer.h
index 4f42c07..79dcefc 100644
--- a/node_modules/react-native-linear-gradient/ios/RNLinearGradientLayer.h
+++ b/node_modules/react-native-linear-gradient/ios/RNLinearGradientLayer.h
@@ -1,11 +1,10 @@
 #import <Foundation/Foundation.h>
 #import <QuartzCore/QuartzCore.h>
-
-@class UIColor;
+#import <React/RCTUIKit.h>
 
 @interface RNLinearGradientLayer : CALayer
 
-@property (nullable, nonatomic, copy) NSArray<UIColor *> *colors;
+@property (nullable, nonatomic, copy) NSArray<RCTUIColor *> *colors;
 @property (nullable, nonatomic, copy) NSArray<NSNumber *> *locations;
 @property (nonatomic) CGPoint startPoint;
 @property (nonatomic) CGPoint endPoint;
diff --git a/node_modules/react-native-linear-gradient/ios/RNLinearGradientLayer.m b/node_modules/react-native-linear-gradient/ios/RNLinearGradientLayer.m
index a2ff879..edd19a3 100644
--- a/node_modules/react-native-linear-gradient/ios/RNLinearGradientLayer.m
+++ b/node_modules/react-native-linear-gradient/ios/RNLinearGradientLayer.m
@@ -1,7 +1,7 @@
 #import "RNLinearGradientLayer.h"
 
 #include <math.h>
-#import <UIKit/UIKit.h>
+#import <React/RCTUIKit.h>
 
 @implementation RNLinearGradientLayer
 
@@ -60,6 +60,7 @@
         hasAlpha = hasAlpha || CGColorGetAlpha(self.colors[i].CGColor) < 1.0;
     }
 
+#if !TARGET_OS_OSX
     if (@available(iOS 10.0, *)) {
         UIGraphicsImageRendererFormat *format;
         if (@available(iOS 11.0, *)) {
@@ -76,13 +77,18 @@
         self.contents = (__bridge id _Nullable)(image.CGImage);
         self.contentsScale = image.scale;
     } else {
+#else // TARGET_OS_OSX
+    {
+#endif // TARGET_OS_OSX
         UIGraphicsBeginImageContextWithOptions(self.bounds.size, !hasAlpha, 0.0);
         CGContextRef ref = UIGraphicsGetCurrentContext();
         [self drawInContext:ref];
 
         UIImage *image = UIGraphicsGetImageFromCurrentImageContext();
-        self.contents = (__bridge id _Nullable)(image.CGImage);
-        self.contentsScale = image.scale;
+//        self.contents = (__bridge id _Nullable)(image.CGImage);
+//        self.contentsScale = image.scale;
+        self.contents = (__bridge id _Nullable) UIImageGetCGImageRef(image);
+        self.contentsScale = UIImageGetScale(image);
 
         UIGraphicsEndImageContext();
     }
@@ -199,7 +205,7 @@
 
     CGColorSpaceRef colorSpace = CGColorSpaceCreateDeviceRGB();
     NSMutableArray *colors = [[NSMutableArray alloc] initWithCapacity:self.colors.count];
-    for (UIColor *color in self.colors) {
+    for (RCTUIColor *color in self.colors) {
         [colors addObject:(id)color.CGColor];
     }
 
diff --git a/node_modules/react-native-linear-gradient/ios/RNLinearGradientManager.mm b/node_modules/react-native-linear-gradient/ios/RNLinearGradientManager.mm
index ed73ede..677a7a2 100644
--- a/node_modules/react-native-linear-gradient/ios/RNLinearGradientManager.mm
+++ b/node_modules/react-native-linear-gradient/ios/RNLinearGradientManager.mm
@@ -1,6 +1,7 @@
 #import "RNLinearGradientManager.h"
 #import "RNLinearGradient.h"
 #import <React/RCTBridge.h>
+#import <React/RCTUIKit.h>
 
 @implementation RNLinearGradientManager
 
@@ -8,7 +9,7 @@ RCT_EXPORT_MODULE();
 
 @synthesize bridge = _bridge;
 
-- (UIView *)view
+- (RCTUIView *)view
 {
   return [[RNLinearGradient alloc] init];
 }
diff --git a/node_modules/react-native-linear-gradient/windows/RNLinearGradient/RNLinearGradient/RNLinearGradient.vcxproj b/node_modules/react-native-linear-gradient/windows/RNLinearGradient/RNLinearGradient/RNLinearGradient.vcxproj
index 5e228da..7870388 100644
--- a/node_modules/react-native-linear-gradient/windows/RNLinearGradient/RNLinearGradient/RNLinearGradient.vcxproj
+++ b/node_modules/react-native-linear-gradient/windows/RNLinearGradient/RNLinearGradient/RNLinearGradient.vcxproj
@@ -13,13 +13,16 @@
     <AppContainerApplication>true</AppContainerApplication>
     <ApplicationType>Windows Store</ApplicationType>
     <ApplicationTypeRevision>10.0</ApplicationTypeRevision>
-    <WindowsTargetPlatformVersion Condition=" '$(WindowsTargetPlatformVersion)' == '' ">10.0.18362.0</WindowsTargetPlatformVersion>
-    <WindowsTargetPlatformMinVersion>10.0.16299.0</WindowsTargetPlatformMinVersion>
   </PropertyGroup>
-  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.Default.props" />
   <PropertyGroup Label="ReactNativeWindowsProps">
     <ReactNativeWindowsDir Condition="'$(ReactNativeWindowsDir)' == ''">$([MSBuild]::GetDirectoryNameOfFileAbove($(MSBuildThisFileDirectory), 'node_modules\react-native-windows\package.json'))\node_modules\react-native-windows\</ReactNativeWindowsDir>
   </PropertyGroup>
+  <Import Project="$(ReactNativeWindowsDir)\PropertySheets\External\Microsoft.ReactNative.WindowsSdk.Default.props" Condition="Exists('$(ReactNativeWindowsDir)\PropertySheets\External\Microsoft.ReactNative.WindowsSdk.Default.props')" />
+  <PropertyGroup Label="Fallback Windows SDK Versions">
+   <WindowsTargetPlatformVersion Condition=" '$(WindowsTargetPlatformVersion)' == '' ">10.0.18362.0</WindowsTargetPlatformVersion>
+   <WindowsTargetPlatformMinVersion Condition=" '$(WindowsTargetPlatformMinVersion)' == '' ">10.0.16299.0</WindowsTargetPlatformMinVersion>
+  </PropertyGroup>
+  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.Default.props" />
   <ItemGroup Label="ProjectConfigurations">
     <ProjectConfiguration Include="Debug|ARM">
       <Configuration>Debug</Configuration>
